#include <vector>
#include <string>
#include <SFML/Graphics.hpp>

#include "Trie.h"

extern sf::RenderWindow *g_window;
extern Trie *g_tries[];
extern Trie *g_curr_trie;
extern std::vector<std::string> g_favorites;
extern std::vector<std::string> g_history;
